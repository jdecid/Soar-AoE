sp {propose*initialize-villager
    (state <s> ^superstate nil -^name)
-->
    (<s> ^operator <o> +)
    (<o> ^name eater
}

sp {apply*initialize-villager
    (state <s> ^superstate nil ^name)
-->
    (<s> ^operator <o> +)
    (<o> ^name eater
}